# Architecture

`app.py` provides the presentation layer. `biodiversity_engine.py` provides validation, scoring and aggregation. CSV files in `data/` provide the local source data. Local visual assets are stored in `assets/`. Tests in `tests/` validate core calculations and schemas.
