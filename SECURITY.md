# Security

Do not commit personal information, exact sensitive nesting locations, secrets, API tokens or credentials.

The app does not require an external API key. Uploaded CSV data is handled in the local application session and is not intentionally sent to a remote service.
